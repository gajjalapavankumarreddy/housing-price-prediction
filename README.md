# housing-price-prediction
Housing price prediction project using Regularized Linear Regression (Ridge &amp; Lasso). The project covers data preprocessing, model training, and hyperparameter tuning by analyzing lambda vs error curves to control overfitting. It demonstrates the bias–variance tradeoff and model performance using real housing data in Python.
